# متطلبات النظام

## المتطلبات الأساسية لتشغيل مشروع قبو الذكاء الاصطناعي المعزز

قبل البدء، تأكد من توفر المتطلبات التالية على جهازك:

---

## 1️⃣ متطلبات النظام الأساسية

### نظام التشغيل
- **Windows**: Windows 10 أو أحدث
- **macOS**: macOS 10.13 أو أحدث (Intel أو Apple Silicon)
- **Linux**: أي توزيعة حديثة (Ubuntu 18.04+، Fedora 30+، إلخ)

### الحد الأدنى للموارد
- **المعالج (CPU)**: معالج حديث (Intel i5 / AMD Ryzen 5 أو أقوى)
- **الذاكرة (RAM)**: 4 GB على الأقل (8 GB موصى به)
- **مساحة التخزين**: 500 MB على الأقل للمشروع الأساسي
- **الاتصال بالإنترنت**: مطلوب للتثبيت والتنزيلات

---

## 2️⃣ البرمجيات المطلوبة

### متصفح ويب (إجباري)
يجب أن يكون لديك متصفح ويب حديث:

- **Google Chrome** (إصدار 90+)
- **Mozilla Firefox** (إصدار 88+)
- **Apple Safari** (إصدار 14+)
- **Microsoft Edge** (إصدار 90+)

### محرر نصوص (إجباري)
أي من المحررات التالية لتحرير الملفات:

- **Visual Studio Code** (موصى به) - مجاني وقوي
- **Sublime Text** - خفيف الوزن وسريع
- **Atom** - مرن وقابل للتخصيص
- **NotePad++** - بسيط وفعال
- أي محرر نصوص آخر

### Node.js (اختياري لكن موصى به)
مطلوب للميزات المتقدمة مثل إنشاء ملف Manifest:

- **النسخة الموصى بها**: Node.js 14.0 أو أحدث
- **التنزيل من**: https://nodejs.org/
- **التحقق من التثبيت**:
  ```bash
  node --version    # يجب أن يظهر v14.0.0 أو أحدث
  npm --version     # يجب أن يظهر 6.0.0 أو أحدث
  ```

### Git (اختياري لكن موصى به)
للحصول على آخر نسخة من المشروع:

- **النسخة الموصى بها**: Git 2.30+
- **التنزيل من**: https://git-scm.com/
- **التحقق من التثبيت**:
  ```bash
  git --version     # يجب أن يظهر git version 2.30+
  ```

---

## 3️⃣ المكتبات والمتعلقات

### المكتبات الأساسية (مع Node.js)

```json
{
  "gray-matter": "^4.0.3"  // لاستخراج بيانات Markdown
}
```

تُثبت تلقائياً عند تشغيل:
```bash
npm install
```

### المكتبات الاختيارية

للميزات المتقدمة، قد تحتاج:

- **Python 3.8+** - للسكريبتات البرمجية
  - `pip install` للحزم المطلوبة
  - تثبيت من: https://www.python.org/

---

## 4️⃣ متطلبات الإنترنت

### التنزيلات المطلوبة
- متصفح ويب: 100+ MB
- Node.js: 100+ MB
- المشروع نفسه: 50+ MB (بعد التنزيل)

### الخوادم والخدمات (اختياري)
إذا كنت تستخدم ميزات متقدمة:

- **خادم محلي**: Python أو Node.js
- **خدمات الذكاء الاصطناعي الخارجية**: اتصال API إلى خوادم خارجية
- **مخزن سحابي**: GitHub، Google Drive، إلخ (اختياري)

---

## 5️⃣ متطلبات الوصول والأذونات

### أذونات الملفات
- **القراءة**: لكل الملفات في المشروع
- **الكتابة**: لمجلد المشروع (لإنشاء manifest.json وملفات أخرى)
- **التنفيذ**: للسكريبتات (generate-manifest.js, build_vault.py)

### الوصول إلى الشبكة
- **متطلب اختياري** لتنزيل التحديثات من GitHub
- **متطلب اختياري** للخدمات الخارجية والمكتبات

---

## 6️⃣ قائمة التحقق قبل البدء

استخدم هذه القائمة للتحقق من أن كل شيء جاهز:

```
☐ نظام التشغيل محدث (Windows 10+, macOS 10.13+, Linux حديث)
☐ الذاكرة متاحة (4 GB على الأقل)
☐ مساحة تخزين كافية (500 MB على الأقل)
☐ متصفح ويب حديث مثبت
☐ محرر نصوص مثبت (VS Code موصى به)
☐ Node.js مثبت (اختياري لكن موصى به)
  ☐ التحقق: node --version
  ☐ التحقق: npm --version
☐ Git مثبت (اختياري لكن موصى به)
  ☐ التحقق: git --version
☐ الاتصال بالإنترنت متاح
☐ لديك أذونات كتابة على مجلد المشروع
```

---

## 7️⃣ التحقق من التثبيت

بعد تثبيت المتطلبات، تحقق من:

```bash
# تحقق من Node.js
node -v
npm -v

# تحقق من Git
git --version

# جرّب استنساخ المشروع
git clone https://github.com/My-Knowledge-Garden/AI-Augmented-Vault.git
cd AI-Augmented-Vault
npm install

# تحقق من أن كل شيء يعمل
node generate-manifest.js
```

إذا رأيت رسائل خطأ، راجع قسم الاستكشاف في `troubleshooting.md`.

---

## 8️⃣ متطلبات إضافية حسب الاستخدام

### 🎵 للعمل مع الموسيقى والصوت
- **برامج مرتبطة** (اختياري): Audacity, FL Studio, Ableton Live
- **مكتبات صوتية** (اختياري): ffmpeg

### 🎬 للعمل مع الفيديو
- **برامج تحرير**: DaVinci Resolve, Adobe Premiere Pro, Final Cut Pro
- **مكتبات الفيديو** (اختياري): ffmpeg, MediaInfo

### 💻 لتطوير البرمجيات
- **لغات برمجة**: Python, JavaScript, إلخ
- **أدوات مطور**: IDE مثل PyCharm, WebStorm
- **نظام التحكم بالإصدارات**: Git

### 🔧 للإصلاحات والصيانة
- **أدوات تشخيص**: توافقة مع جهازك
- **برامج مساعدة**: System Utilities حسب الحاجة

---

## 📋 ملخص المتطلبات

| المتطلب | النوع | الحد الأدنى | الموصى به | الاختياري |
|---------|-------|-----------|----------|---------|
| نظام التشغيل | إجباري | Windows 10 / macOS 10.13 | Windows 11 / macOS 12 | - |
| الذاكرة | إجباري | 4 GB | 8 GB | 16 GB+ |
| المعالج | إجباري | i5 / Ryzen 5 | i7 / Ryzen 7 | - |
| متصفح | إجباري | Chrome 90+ | أحدث نسخة | - |
| محرر نصوص | إجباري | أي محرر | VS Code | - |
| Node.js | اختياري | 14.0 | 18.0+ | - |
| Git | اختياري | 2.30 | 2.40+ | - |
| Python | اختياري | 3.8 | 3.10+ | - |

---

## 🤔 أسئلة شائعة

**س: هل يمكنني استخدام المشروع بدون Node.js؟**
ج: نعم، يمكنك تصفح المحتوى واستخدام الملفات الأساسية. لكن Node.js مطلوب لإنشاء manifest.json.

**س: هل يعمل المشروع بدون إنترنت؟**
ج: نعم، بعد التنزيل الأول. لكن التنزيل الأولي والتحديثات تتطلب إنترنت.

**س: ما أفضل محرر نصوص؟**
ج: Visual Studio Code موصى به بشدة - مجاني وقوي وسهل الاستخدام.

---

**للمتطلبات التفصيلية أو مشاكل التثبيت، راجع `installation-guide.md` و `troubleshooting.md`**

---

**آخر تحديث**: 30 أبريل 2026
